# DeepSeek API Key（必须）
DEEPSEEK_API_KEY=sk-your-key-here

# 数据存储目录（可选，默认 ./data/）
# 云端部署建议设为持久化磁盘路径，如 /var/data/
DATA_DIR=./data

# 数据库文件路径（可选，默认 ./cat_assistant.db）
# 云端部署建议与 DATA_DIR 在同一持久化磁盘下
DB_PATH=./cat_assistant.db

# 服务端口（可选，默认 8001，云端自动分配）
PORT=8001
